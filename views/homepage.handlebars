{{#each posts as |post| }}

<div class="text-center">

  <h2>{{post.title}}</h2>
  <h3>{{post.content}}</h3>
  <h4>Posted by: {{user.username}} on {{format_date date_created}}</h4>
  
</div>

{{/each}}
{{!-- <button class="post-delete" data-id="{{id}}">Delete</button> --}}

<script src="/js/post.js"></script>